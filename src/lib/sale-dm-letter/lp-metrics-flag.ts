// 2026-09-11 公開LP(PR3)で /t/ が LP型ごとにページを出すようになったため解禁。
// これまでは全員が同じ外部LPを見ており、LP型別の閲覧は「ページの成績」ではなかった(@codex R3 P1)。
// 今は QR の遷移先が LP型ごとに変わるため、LP型ごと/組み合わせの集計に意味が出た。
//
// ⚠画面と API の**両方**がこの1か所を見る(@codex R4 P2)。画面だけ隠して API が出し続けると、
//   まだ意味を持たない数字が JSON に載ったまま配られる。
export const LP_METRICS_ENABLED = true;
